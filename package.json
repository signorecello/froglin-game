{
  "name": "froglin_ts",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "yarn workspaces foreach -A run start",
    "test": "yarn test::dry ${0}",
    "test::dry": "DRY_RUN=true vitest ${0}"
  },
  "packageManager": "yarn@4.1.1",
  "devDependencies": {
    "vitest": "^1.4.0"
  },
  "dependencies": {
    "@types/node": "^20.12.2",
    "@zk-kit/eddsa-poseidon": "^0.6.0",
    "@zk-kit/smt": "https://gitpkg.now.sh/signorecello/zk-kit/packages/smt?zpedro/smt_pedersen"
  }
}
